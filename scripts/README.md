# Scripts sugeridos

Este repositorio prioriza comandos explícitos por semana para facilitar la enseñanza. Puedes agregar scripts `.ps1` o `.sh` según el sistema operativo usado por la clase.

Ejemplo PowerShell:

```powershell
cd Modulo1/Semana1/src/Inventory.Api
dotnet run
```
